apiVersion: v1
kind: Service
metadata:
  name: {{ include "a8s-admin-frontend-chart.fullname" . }}
  labels:
    {{- include "a8s-admin-frontend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a8s-admin-frontend-chart.selectorLabels" . | nindent 4 }}
